### Alice repository: verify and publish pending work

Checked the active feature branch, working tree, and refreshed remote state before publication. The checkout contained no pre-existing uncommitted or unpushed changes.

### Alice documentation: publish to main

Recorded the repository-specific decision that documentation changes may be merged directly into `main`, then prepared the existing feature branch for a fast-forward publication.

### Alice becomes the home of Jayverse planning docs (moved from Rabbit)

jay decided Alice is the repo for documenting and planning the Jayverse services and for tech notes. Rabbit's `docs/features/`, `docs/tasks/`, `docs/history/`, and `docs/memo.md` now live here as the canonical copies; Rabbit keeps only its portal-operations docs (`rabbit-design.md`, `runbooks/`, `handbook/`) and the frozen notes site. Synced the files that had changed in Rabbit since the 09-12 snapshot (the features hub README with the three end-to-end scenarios, seven newer history files) and merged Rabbit's own 09-12 and 09-13 entries into the same-named daily files here.

### Alice: render its own HTML mirror

Brought over Rabbit's `scripts/generate-docs-html.mjs`, `rtd-shell.mjs`, `generate-logs-html.mjs`, and the `.githooks/pre-commit` hook, with a minimal `package.json` (`marked` only) so `docs/html/` can be regenerated here instead of being a frozen snapshot. Ran a full regeneration: 318 markdown files, tasks and history indexes, `docs/logs.html`. Enable the hook per checkout with `git config core.hooksPath .githooks`.

### Alice landing pages: cards point at local copies again

The Memo, Feature Designs, Current Plan, History, and Docs cards on `index.html` and `docs/index.html` were linking to Rabbit on GitHub; now that the files are canonical here they link to the local rendered pages. Added the DeFi (EtherFi study) card that Rabbit's index had gained on 09-11.
