# 2026-09-16

### Alice Dev Notes: adapt wallets to two native account-abstraction standards

Added a bilingual Important report on the EIP-8130/EIP-8141 split and its wallet-layer solution: one intent model, versioned per-chain capability manifests, separate adapters, explicit fallbacks, normalized receipts, and cross-adapter conformance tests. Recorded both EIPs as Draft and distinguished the reported direction from final deployment.

### Alice Dev Notes: turn articulation into a repeatable idea system

Added a bilingual Important report that converts Dan Koe's articulation advice into an inspectable idea-card library, three reusable composition formats, a daily retrieval loop, and quality metrics. Linked the original article and readable mirror while summarizing rather than reproducing the source.

### Alice landing pages: "Knowledge Base" → "Dev Knowledge Base", Project Management first

jay asked to rename the section and lead it with the Project Management card. Renamed the section title on both landing-page copies (`index.html`, `docs/index.html`) and moved the `management.md` card to the head of the grid; no cards added or removed. Source: jay's request in conversation, no task file.

### Project Management: "Code Quality in the AI Era" section from the Tech Bridge / IBM video

Appended a brief section to `docs/knowledge/management.md`: the video's thesis (writing code is cheap, decision quality is the differentiator), five takeaways with timestamps, and the six-step feature workflow where AI writes the baseline and humans govern the system; tied steps 4–5 to the file's existing "no code review except critical parts" rule and to the Jayverse invariant monitor. Content supplied by jay from the video; condensed, not reproduced.

### Rename "Dev Notes" → "Tech Notes"

jay asked for the rename. Applied to the notes page (title, filter placeholder), every topic detail page (title suffix, breadcrumb, "All Tech Notes" links; Korean "전체 기술 노트"), and the notes card on both landing pages. Left untouched: the separate "Dev Notes 2" card (`docs/zsub/dev2.md`, a different document) and past history entries, which are append-only. Source: jay's request in conversation, no task file.

### Tech Notes rail: current status in red, previous day's closing status in blue

jay asked to replace the pink "2%/day → 95% done" pace badge with yesterday's status in blue and make the current status red. Added a day-by-day `progress` log to `docs/topics/_nav.js` (09-14: 59/412, 09-15: 59/414, read from the last `notes.html` commit of each KST day) and a shared `docs/topics/_progress.js` that sums today's per-section counts for the red badge and shows the latest log entry dated before today, labelled with its date, as the blue one. Loaded by `notes.html` and all 264 topic pages that carry the rail head, which also fixes the topic pages' static overall badge (260 of them still said 58/412). Rule going forward: when counters are bumped on a new KST day, append the previous day's closing numbers to `progress` first. Source: jay's request in conversation, no task file.
