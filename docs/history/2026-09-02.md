### Rabbit workspace: restrict edits to work-agent docs

Added a global workspace rule requiring Rabbit tasks to use `/Users/jay/work-agent/rabbit` and limiting all repository edits to its `docs/` directory. Marked other repository paths and the alternate checkout as read-only.

### Verex workspace: allow docs-only edits

Extended the global workspace boundary to permit edits in `/Users/jay/work-agent/verex/docs/`. All other paths in the Verex checkout remain read-only.

### Rabbit checkout: verify synchronization

Compared the work-agent and work checkouts and confirmed that both already point to `aa193cf`. No repository content needed synchronization.

### Blockchain PoCs: add three finality and RPC priorities

Added three blue important items covering receipt-versus-settlement state, the multiple clocks behind L2 finality, and RPC responses as node views rather than consensus. Added bilingual detail pages with small, verifiable PoC scopes and primary protocol references.

### Blockchain PoCs: add application and library priorities

Added three additional blue important cards centered on Ponder reorg handling, embedded-wallet authority and recovery, and viem transaction simulation. Kept each as a runnable application-layer experiment rather than a protocol explainer.

### Blockchain PoCs 23–24: mark discussion complete

Changed the Quick Slots and unified transaction-content-floor items from important/new to black done markers across the catalogue and generated detail-page navigation. Updated the aggregate and Blockchain completion counts to match.
