### Jayverse game deployment safety check

Verified the live integration branch, current `main`, Cloud Run configuration, and public routes before deployment. Deployment was paused because `claude/chat-session-uzubhl` is based on an August snapshot and would roll back substantial current Rabbit work if deployed directly.

### Jayverse game: integrate onto current Rabbit

Transplanted only the game commit onto current `main`, preserving newer Rabbit code. Made the Game tab public, moved the pinned submodule to `games/jayverse-game`, switched the static bundle to a Docker-generated artifact, and kept Coin Catcher as the missing-bundle fallback.

### Jayverse game: verify builds and routes

The isolated Next 16 game export, Rabbit TypeScript check, and full Next 14 production build passed. Logged-out checks returned 200 for `/game`, bare `/jayverse-game`, `/street`, `/settlement`, and a hashed asset; the temporarily bundle-free page also showed the fallback and build command.

### Jayverse game: deploy to Cloud Run

Published integration commit `402e380` to `main` and deployed Cloud Build `312fbdaf-c250-4161-b01b-d3e226d5e226` as Cloud Run revision `rabbit-00133-nsn` with 100% traffic. Confirmed `ALLOW_GAME=true`, canonical `AUTH_URL`, the iframe, public Game navigation, game routes, canonical base-path redirect, and a generated JavaScript asset in production.

### Jayverse game: stack dialogue into two rows

Changed the fixed dialogue card so Jay's identity and line emoji share the first row while the message uses a full-width second row. This prevents the message from collapsing into a narrow column on mobile; the isolated static-export build passed.

### Dev notes: add Graph & Loop Engineering technique

Added Google's two-hour Graph & Loop Engineering course map to `docs/dev.md`, including the progression from a first agent graph through parallel agents, control loops, and self-improving runtime graphs. Highlighted the practical `route → check → repeat/rebuild` pattern as the alternative to continually expanding one agent's prompt.

### Dev notes: correct target page

Removed the Graph & Loop Engineering item from `docs/dev.md` after Jay clarified that the intended destination was the generated Dev Notes page at `docs/notes.html`. Confirmed that its canonical card data is maintained in `lib/poc-cards.ts`, which remains read-only under the current workspace boundary pending explicit approval.

### Dev Notes: add Graph & Loop Engineering card

After Jay approved the canonical-source edit, added an Important bilingual card covering the course timeline, graph routing, bounded checker loops, parallel fan-out/fan-in controls, and gated self-improvement. Regenerated the Dev Notes index and topic detail page from the card data.

### Dev Notes: publish Graph & Loop Engineering card

Validated the generated Important entry, bilingual detail page, navigation, TypeScript, and whitespace checks. Prepared the scoped card and documentation outputs for publication from `agent-parked` to `origin/main` after Jay approved commit and push.

### Jayverse dialogue: deploy the mobile layout

Deployed Cloud Build `3ef3deb9-caed-45db-893f-362a4da0e55c` as Cloud Run revision `rabbit-00136-qk4` with 100% traffic. The live `/game` page returned 200, and its generated JayVerse chunk contains both the two-row speaker wrapper and column layout rule.

### Jayverse settlement: remove the header back button

Removed the visible “Back to the street” header button together with its translations and unused CSS while preserving the in-scene return tunnel. The isolated JayVerse static-export build passed, and the generated bundle contains no removed button text or class.

### Alice: create non-destructive Rabbit docs snapshot

Created the Alice repository with a complete copy of Rabbit's `docs/` tree, including frozen content, and copied `docs/index.html` to the repository root as its landing page. Added a root-only base path so that landing-page links resolve into the preserved `docs/` snapshot. Rabbit remained unchanged; Alice started on `Codex/alice-content` with no commit so the full snapshot can be reviewed first.

### Alice: create private GitHub remote

Created the private `linked0/alice` GitHub repository and attached it to the local Alice checkout as `origin`. Kept the initial snapshot uncommitted and unpushed in accordance with the review-before-commit policy.

### Alice: publish the documentation snapshot with GitHub Pages

Prepared the copied documentation for `https://linked0.github.io/alice/` by keeping the root landing page pointed at `docs/` and adding `.nojekyll` so generated underscore-prefixed assets remain available. Published the reviewed snapshot through `Codex/alice-content` to remote `main` and configured Pages from the repository root.

### Alice: redact credentials before public publication

Replaced copied MCP/API credential values in Alice's `docs/zsub/settings.json` with `REDACTED` placeholders before the first push. The corresponding Rabbit file was not changed, and no credential-bearing Alice commit was published remotely.

### Alice: link Rabbit cards to the source repository

Updated the Memo, Feature Designs, Current Plan, History, and Docs cards on both Alice landing-page copies to open their canonical files or folders in `linked0/rabbit`.

### Alice: align Rabbit card links with their displayed paths

Changed the Feature Designs card from the `docs/features/` folder to the exact displayed file, `docs/features/README.md`. The remaining cards already pointed to the files or folders printed on their labels.

### Jayverse settlement: deploy without the back button

Deployed Cloud Build `567c26c4-a8aa-41e9-99f6-200958e9b967` as Cloud Run revision `rabbit-00139-g2l` with 100% traffic. Verified live 200 responses for `/game` and `/jayverse-game/settlement`; the production bundle contains none of the removed button text or class while retaining the animated return tunnel, and the revision has no error-level log entries.
