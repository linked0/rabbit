### Jayverse game deployment safety check

Verified the live integration branch, current `main`, Cloud Run configuration, and public routes before deployment. Deployment was paused because `claude/chat-session-uzubhl` is based on an August snapshot and would roll back substantial current Rabbit work if deployed directly.

### Jayverse game: integrate onto current Rabbit

Transplanted only the game commit onto current `main`, preserving newer Rabbit code. Made the Game tab public, moved the pinned submodule to `games/jayverse-game`, switched the static bundle to a Docker-generated artifact, and kept Coin Catcher as the missing-bundle fallback.

### Jayverse game: verify builds and routes

The isolated Next 16 game export, Rabbit TypeScript check, and full Next 14 production build passed. Logged-out checks returned 200 for `/game`, bare `/jayverse-game`, `/street`, `/settlement`, and a hashed asset; the temporarily bundle-free page also showed the fallback and build command.
