### Rabbit repository: change visibility to public

Reviewed tracked files and Git history for obvious secret formats, treating the configured wallet and service values as test placeholders per Jay's confirmation. Changed `linked0/rabbit` from private to public so Alice's source links work without GitHub authentication; no application code was changed.
