# 2026-06-24

### sub-agents.md: add OneNote MCP setup section
- Added section 11 to `docs/sub-agents.md` documenting how to connect the `danosb/onenote-mcp` server to Claude Desktop (clone → `npm start` → register `node` + absolute `.mjs` path, with JSON config example).
- Why: it follows the same absolute-path MCP wiring pattern already used elsewhere.
