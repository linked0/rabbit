# 2026-09-22 — Knowledge Notes (alice) dev notes

Source docs this day's work implements: [`docs/topics/README.md`](../topics/README.md) (the schema),
[`docs/topics/raw/README.md`](../topics/raw/README.md) (the raw layer), and for the first entry the
Gemini briefing at [`docs/topics/raw/2026-09-22-gemini-youtube.md`](../topics/raw/2026-09-22-gemini-youtube.md).
Yesterday's file: [`2026-09-21-dev-notes-history.md`](2026-09-21-dev-notes-history.md).

### Eng #399 pushed, and merge/push becomes standing

- **Cause:** a commit sat unpushed on `main` from 08:08 — Eng #399, a flapping node, for talking about intermittent bugs. It had also gone straight onto `main` rather than through a branch. jay then said: "You can merge and push at your will from now on."
- **Change:** the commit was put on a branch, pushed, `main` pushed, branch deleted — a fast-forward, so the history is identical to a direct push but the shape matches every other change. The standing authorisation is saved to memory; it supersedes the global rule that pushes need an explicit ask, for directed work only. Rewriting published history, force-pushes and deletions still stop and ask.
- **Result:** Eng 399 items, the new one shown at #2375. The worktree branch `claude/number-benchmark` was checked at the same time: fully merged, nothing stranded.

### Tech #72: assistance is optimised to look right

- **Cause:** jay pasted a Gemini summary of an AI Engineer conference talk arguing that today's models fail at unattended automation by design, and asked for it as an item.
- **Reasoning:** the claim worth keeping is not "models are unreliable" but that the unreliability has a named cause you did not choose and cannot see — RLHF scores answers by human preference, so a plausible wrong answer beats an honest "I don't know", and confidence becomes a style rather than a signal. That is yesterday's Tech #72 lesson one level down: there a tool's own "high-confidence" label was not a measurement, here the model's confident tone is not one either, and in both cases the label came from the thing being judged. So the item is built around a calibration harness — fifty known-answer questions, answer plus stated confidence, bucketed and compared against actual accuracy — because a flat curve means there is no threshold to automate behind, and no prompt fixes that. The proposed cure, a third post-training path beside preference and verifiable reward, is a company's direction with no published results and is marked as such.
- **Change:** `docs/topics/pocs-assistance-optimised-to-look-right.html` at Tech #72 (type Talk, bin `deep`, source `gemini`), with a three-target comparison table, the five-step harness, a curve-reading block, the Jayverse landing and 16 key expressions. `raw/2026-09-22-gemini-youtube.md` keeps the citation and the timestamped claims rather than the generated prose, since the URL plus the claims are what make the source reopenable.
- **Result:** Tech 282 items, site total 925. The speaker's name and company as transcribed are flagged unverified — machine transcription garbles names, and the page points at the video rather than asserting them.

### Tech #72: three ways to put the won on a chain

- **Cause:** jay pasted a Korean strategy summary arguing that Korean banks should prepare three tracks at once — tokenised deposit, bank-issued stablecoin, third-party stablecoin — and asked for it as an item.
- **Reasoning:** summarising the three tracks would have added nothing, so the item is built on the one question they are three answers to: whose liability is the money. A tokenised deposit leaves the claim where it was and changes only the rail, which is why insurance, interest and the lending relationship survive; a bank-issued coin is a new liability wearing the same name; distributing someone else's coin takes the claim off the balance sheet entirely. That is the generalisation of the keeper from Tech #64 — a payment rail is a balance-sheet position before it is software. Two things the briefing does not say got written in. The second track works **against** the stated goal: reserves backing a bank's own coin come out of the deposit base, so a customer moving from deposit to bank coin has left it just as surely as if they had gone elsewhere, and only the first track is purely defensive. And the two-tier Hangang design maps exactly onto the money hierarchy that already exists — deposit token to deposit, wholesale CBDC to central-bank reserves — which is both the argument for it working and the reason to doubt claims that it changes anything fundamental. A fourth note: permissioned versus public is an access-control choice here, not a technology one, since a KYC whitelist closes a public chain at the account layer, which is the layer a regulated liability cares about.
- **Change:** `docs/topics/pocs-three-tracks-won-onchain.html` at Tech #72 (type Brief, bin `deep`), with a whose-liability table, the two-tier mapping, a Saturday-2am failure test, the Jayverse landing and 16 key expressions. `raw/2026-09-22-korean-bank-deposit-tokens.md` keeps the claims and numbers; **no URL was captured**, so unusually for the raw layer the source cannot be reopened at all, and the file says so.
- **Result:** Tech 283 items, site total 926. Every figure is marked unverified, including the roughly 110 trillion won of annual subsidies and the 60 trillion said to move offshore, as is the attribution of the framing to "Tempo", which the summary never identifies. The PoC is a whose-liability ledger per track — the same shape as the Auditor's authority matrix, with a column it currently lacks.

### The grey badge was five days stale, because keeping it fresh was a manual rule

- **Cause:** jay, with a screenshot of the two rail badges: the red one read 8% · 71/925 and the grey one beside it still read 14% · 61/442. That grey figure is the previous day's closing status, and its tooltip dated it 2026-09-17.
- **Reasoning:** `_progress.js` reads `nav.progress`, a day-by-day log, and shows the latest entry dated before today. The log was maintained by a rule written in the file's own comment — on a new KST day, append the previous day's closing numbers before changing the counters — and that rule had not been followed since the 17th. So the badge was not broken; it was faithfully displaying a log that had stopped being written. A rule a human has to remember on exactly the right day is a rule that will lapse, and this one lapsed for five days while the totals moved from 442 items to 926. The fix is to remove the rule rather than try harder to follow it: upsert **today's** totals on every run instead of trying to catch the moment a day ends. By the end of a day that entry holds the day's closing numbers, and tomorrow it is automatically the latest entry before today. A day with no work simply leaves no entry and the badge falls back to the last day that had any, which its tooltip names.
- **Change:** `roll-done-states.py` upserts today's totals into `nav.progress` on every run, so any item added or status changed keeps the log current. The four missing days were reconstructed from git — the last commit of each KST day that touched `_nav.js` — and appended; jay's four hand-entered days were left exactly as he recorded them, including where they differ from the git-derived figure by an item.
- **Result:** the grey badge now reads 8% · 71/923 dated 2026-09-21 instead of 14% · 61/442 dated 2026-09-17, and it will stay current without anyone remembering to update it. The log also became useful in its own right: 442 items on the 16th, 923 on the 21st, 926 today, with done stuck at 68 through the 19th and 20th.

### Sign-in failed locally, and one error code hid two different causes

- **Cause:** jay, with a screenshot of the local page: tapping **Sign in** under the rail printed `auth/unauthorized-domain` in red. Asked separately for the same control at the top of a detail page, beside the 한국어 pill.
- **Reasoning:** rather than reason about it, I drove real Chrome at three origins and watched what happened. `http://localhost:4173` signs in fine — `localhost` is an authorized domain by default. `http://127.0.0.1:4173` and a `file://` page both fail, and both report **the same** `auth/unauthorized-domain`, for causes that have nothing to do with each other: 127.0.0.1 is simply a different origin that nobody authorized, while a `file://` page can never work at all, because the SDK requires an http(s) origin (Chrome's console says so; Firebase's error code does not). So the bare code was actively misleading — it named a fixable condition in the case that is not fixable. The fix is therefore two-part: give jay a one-command server on `localhost`, and make the overlay say which of the two it is hitting and what to do about it. On the Edit button: the detail page already has a pill row in the hero, so the button inherits `.lang-switch a` and needs no CSS in 776 generated pages, and the control bar itself moves to the hero where a hero exists — marking an item done should not mean scrolling to the rail foot.
- **Change:** on branch `claude/local-signin-and-hero-edit` in alice. `scripts/serve.sh` serves the **repo root** (what Pages serves, so local paths match alice.jaylabs.xyz) at `http://localhost:4173` and opens the notes page. `docs/topics/_status.js` gains `explain()`, which reports the file:// case and the unauthorized-host case separately and names the host, plus popup-blocked and popup-closed; `heroSwitch()`/`host()` put the Edit pill beside 한국어 and render the control bar in the hero, falling back to the rail foot on notes.html, which has no hero.
- **Result:** verified in Chrome, not merely parsed. Detail page: hero pills read English · 한국어 · Edit, the in-article pill rows are untouched, the bar lands inside the hero and the pill removes itself. `notes.html?list` and the Eng deep link `?tier=all&sec=nav-sec-english` still use the rail foot, still filter (5 sections and 1 section respectively), no page errors. The two failing origins now read "127.0.0.1 is not an authorized domain — open it on http://localhost:4173…" and "sign-in needs an http page — serve the repo (scripts/serve.sh)…". Adding `127.0.0.1` to Firebase's authorized domains would make that spelling work too; the API call to do it was declined by the permission prompt, so it is a one-click job in the console and is noted in the code rather than done.
