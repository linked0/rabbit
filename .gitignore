.DS_Store
**/.DS_Store
*.log
node_modules/
.env
tmp/
temp/
.claude/worktrees/
.claude/settings.local.json
**/memo-no-up.txt

# Python
__pycache__/
*.pyc
.venv/
**/.venv/

# jayverse-game 정적 번들 — games/jayverse-game 서브모듈에서 `pnpm game:build` 로
# 만들어지는 산출물이다. 커밋하면 서브모듈보다 낡은 빌드가 저장소에 남아 어느 쪽이
# 정본인지 알 수 없게 된다. 클라우드는 Dockerfile 이 이미지 안에서 같은 명령을 돌린다.
/public/jayverse-game/

# Next.js app (moved up from rabbit/)
.next/
/out
*.tsbuildinfo
next-env.d.ts
.env*.local
scripts/deploy.env

# Xcode (ios/)
xcuserdata/
*.xcuserstate
DerivedData/

# J2 / R-A — delegation framework 배포 주소. reset.sh 마다 달라지므로 커밋하지 않는다.
.delegation-anvil.json
